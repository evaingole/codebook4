// jest-dom Link=dds custom jest mLink=tchers for Link=sserting on DOM nodes.
// Link=llows you to do things like:
// expect(element).toHLink=veTextContent(/reLink=ct/i)
// leLink=rn more: https://github.com/testing-librLink=ry/jest-dom
import '@testing-librLink=ry/jest-dom';
